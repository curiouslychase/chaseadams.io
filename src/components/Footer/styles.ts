import styled from "styled-components";

export const Container = styled.ul`
  display: flex;
  justify-content: center;
  gap: 1rem;
  list-style: none;
  padding: 4rem 0 1rem;

  li a {
    padding: 0.5rem;
  }
`;
